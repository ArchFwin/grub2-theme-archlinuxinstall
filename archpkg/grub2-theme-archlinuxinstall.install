post_install(){
  cat << _EOF 

==> Add theme to your /etc/default/grub : "GRUB_THEME="/boot/grub/themes/Archlinux/theme.txt", and set the correct screen resolution: "GRUB_GFXMODE=640x480"
==> Update grub by running "grub-mkconfig -o /boot/grub/grub.cfg" as root.

_EOF
}
